OLLAMA_URL=http://localhost:11434
COMFYUI_URL=http://192.168.1.100:8188
OUTPUTS_DIR=./outputs
DATABASE_URL=sqlite+aiosqlite:///./refine_agent.db

# Vision model for image analysis (extraction, critique) - requires vision capability
# Options: moondream (fast, 1.8b), llava:7b, llama3.2-vision:11b (slow but accurate)
VLM_MODEL=moondream

# Text model for prompt generation (faster, no vision needed)
TEXT_MODEL=llama3.2:3b
